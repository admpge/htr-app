import { AppBar, Toolbar, Typography } from '@mui/material';

const Header = () => (
    <AppBar position="static">
        <Toolbar>
            <Typography variant="h5">Handwritten Note Digitizer</Typography>
        </Toolbar>
    </AppBar>
);

export default Header;
