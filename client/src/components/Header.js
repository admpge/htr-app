const Header = () => (
    <header className="bg-gradient-to-r from-blue-500 to-green-500 py-4">
        <div className="container mx-auto flex justify-between items-center">
            <h1 className="text-white text-xl font-semibold">Handwritten Note Digitizer</h1>
            {/* If you have navigation or other content to add to the header, it can go here */}
        </div>
    </header>
);

export default Header;
